{
  "name": "taskmanagement-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm run install-all && npx husky && echo npx lint-staged > .husky/pre-commit",
    "start": "npm run dev",
    "all": "npm run setup && npm start"
  },
  "lint-staged": {
    "backend/**/*.js": [
      "eslint --config backend/.eslintrc.json --fix"
    ],
    "frontend/src/**/*.{js,jsx}": [
      "eslint --config frontend/.eslintrc.json --fix"
    ]
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "selenium-webdriver": "^4.41.0"
  }
}
